#!/bin/sh
#
# Starts Treadmill running container monitor
# Runs as root.
#

exec \
    {{ treadmill }}/bin/treadmill34 \
    sproc --cgroup . \
    monitor cleaning \
        --approot {{ dir }} \
        -S {{ dir }}/cleaning
